# Deprecated compatibility file.
#
# The active online SLAM workflow no longer uses slam_toolbox. It now relies on
# RGB-D sensing with RTAB-Map through:
#   ros2 launch ar_project online_async_launch.py
#
# This placeholder is intentionally scan-free so the repository no longer keeps
# obsolete sensor-specific mapper parameters around.
deprecated_mapper_config:
  backend: rtabmap_rgbd
  active_launch: online_async_launch.py
