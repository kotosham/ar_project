<?xml version="1.0"?>
<model>
  <name>occluder_screen</name>
  <version>1.0</version>
  <sdf version="1.7">model.sdf</sdf>
  <description>Static dark-grey free-standing partition: board 0.90 x 0.06 x 1.00 (centre
  z 0.50) on two 0.10 x 0.30 x 0.04 feet. Spawned in front of a target to hide it from the
  robot's 0.13 m camera so the planner has to change viewpoint instead of failing.</description>
</model>
