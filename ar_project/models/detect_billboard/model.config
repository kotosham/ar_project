<?xml version="1.0"?>
<model>
  <name>detect_billboard</name>
  <version>1.0</version>
  <sdf version="1.7">model.sdf</sdf>
  <description>Vertical billboard textured with ultralytics bus.jpg so an open-vocab
  detector (YOLOE) reliably recognises a "bus"/"person" target in the Gazebo
  render (FLAT DETECT-APPROACH validation; real meshes render undetectably).</description>
</model>
