# Deploy: never commit the filled-in host/credential config (keep only the example).
deploy/build/deploy.env
*.env
!*.env.example

# colcon / ROS build artifacts (root-anchored so deploy/build/ is NOT matched)
/build/
/install/
/log/

# Python bytecode (never commit compiled caches)
__pycache__/
*.py[cod]
